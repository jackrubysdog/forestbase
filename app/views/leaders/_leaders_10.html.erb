<% @apps = Goal.select("goals.player_id,count(goals.player_id) as apps").group("goals.player_id").order("apps DESC").limit(25) %>
<% @a_apps = Goal.select("goals.player_id,count(goals.player_id) as apps").joins(:player).where("players.active=1").group("goals.player_id").order("apps DESC").limit(25) %>
<%= render 'leaders/playerstable' %>